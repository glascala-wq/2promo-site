---
// Catalogo completo gara: fuori dai 3 kit, CTA a preventivo classico.
// Dove esiste una foto prodotto anonimizzata pulita (fondali, gazebo, gonfiabili) la usiamo,
// trattata in duotone navy/cream per stare nella stessa palette delle icone. Per gli item senza
// foto disponibile (banchetti, segnaletica, tovaglie/bandiere) usiamo un'icona originale disegnata
// da zero (GolfIcon.astro), mai una foto fornitore riadattata. Dettaglio in verticale-golf/mapping-prodotti.md.
import GolfIcon from './GolfIcon.astro';

const items = [
  { name: 'Fondali premiazione e media wall', type: 'photo', image: '/golf/products/fondale-premiazione.webp' },
  { name: 'Gazebo hospitality', type: 'photo', image: '/golf/products/gazebo-hospitality.webp' },
  { name: 'Banchetti segreteria', type: 'icon', icon: 'banchetto', tile: 'bg-orange-pale' },
  { name: 'Segnaletica percorso', type: 'icon', icon: 'segnaletica', tile: 'bg-orange-pale' },
  { name: 'Gonfiabili', type: 'photo', image: '/golf/products/gonfiabile-arco.webp' },
  { name: 'Tovaglie e bandiere istituzionali', type: 'icon', icon: 'tovaglia', tile: 'bg-orange-pale' },
];
---
<section id="catalogo" class="bg-cream py-24 md:py-28 border-t border-ink/10">
  <div class="max-w-6xl mx-auto px-6">
    <div class="flex items-center gap-3 mb-6" data-reveal>
      <span class="eyebrow text-ink/70">catalogo completo gara</span>
    </div>
    <h2 class="font-display-tight text-4xl md:text-6xl leading-[1] mb-6 max-w-3xl" data-reveal>
      Tutto quello che serve per la gara, oltre ai kit.
    </h2>
    <p class="text-lg text-ink/70 max-w-2xl mb-14" data-reveal>
      Fondali per la premiazione, gazebo hospitality, banchetti segreteria, segnaletica percorso, gonfiabili, tovaglie e bandiere istituzionali. Un solo fornitore, un solo ordine, consegna confermata per la data gara.
    </p>
    <div class="grid grid-cols-2 md:grid-cols-3 gap-6 mb-14">
      {items.map((item) => (
        <div class="bg-white border-2 border-ink/10 rounded-2xl overflow-hidden" data-reveal>
          {item.type === 'photo' ? (
            <div class="aspect-[4/3] bg-gray-light">
              <img src={item.image} alt={item.name} class="w-full h-full object-cover" width="600" height="450" loading="lazy" />
            </div>
          ) : (
            <div class={`aspect-[4/3] ${item.tile} flex items-center justify-center`}>
              <GolfIcon name={item.icon} class="w-16 h-16 md:w-20 md:h-20 text-ink" />
            </div>
          )}
          <div class="p-4">
            <p class="font-display text-sm text-ink leading-snug">{item.name}</p>
          </div>
        </div>
      ))}
    </div>
    <a
      href="/preventivo"
      class="btn-orange inline-block bg-orange text-cream px-8 py-4 text-sm font-bold tracking-wide rounded-full hover:bg-ink transition"
      style="text-decoration:none;"
    >
      Richiedi Preventivo Catalogo Completo →
    </a>
  </div>
</section>
